{
  "name": "restaurante-erp",
  "version": "1.0.0",
  "description": "Sistema ERP completo para restaurante con pedidos, reservas y gestión",
  "scripts": {
    "dev:all": "concurrently \"npm run dev:backend\" \"npm run dev:cliente\" \"npm run dev:mesero\" \"npm run dev:cocina\" \"npm run dev:admin\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:cliente": "cd frontends/cliente && npm run dev",
    "dev:mesero": "cd frontends/mesero && npm run dev -- -p 3001",
    "dev:cocina": "cd frontends/cocina && npm run dev -- -p 3002",
    "dev:admin": "cd frontends/admin && npm run dev -- -p 3003",
    "install:all": "npm install && cd backend && npm install && cd ../frontends/cliente && npm install && cd ../mesero && npm install && cd ../cocina && npm install && cd ../admin && npm install",
    "build:all": "cd backend && npm run build && cd ../frontends/cliente && npm run build && cd ../mesero && npm run build && cd ../cocina && npm run build && cd ../admin && npm run build",
    "start:backend": "cd backend && npm start",
    "db:start": "cd database && docker-compose up -d",
    "db:stop": "cd database && docker-compose down",
    "db:logs": "cd database && docker-compose logs -f"
  },
  "keywords": [
    "restaurante",
    "erp",
    "pwa",
    "pedidos",
    "reservas",
    "nodejs",
    "react"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
